includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    level: 5

    paths:
        - app
        - database

    ignoreErrors:
        # Example: ignore missing mixin annotations
        - '#Mixed#'

